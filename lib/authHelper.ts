
import {
  LoginFormState,
  RegisterFormState,
} from "@/types/authType";



export const initialRegisterState: RegisterFormState = {
  success: false,
  errors: {},
};

export const initialLoginState: LoginFormState = {
  success: false,
  errors: {},
};
